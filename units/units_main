#Imports
import cv2
import sys
from run_create_units import run_create_units

#Fancy imports from another directory (not sure if I'm using "directory" correctly)
sys.path.append("graph")
import parse_file
sys.path.append("gui")
import creating_gui

#Initialize data
map_raw = open("data/map_data.csv", "r")
starting_units_file = open("data/starting_territories.csv", "r")
image_file = "data/map_w_dots.png"

"""
Call Functions:
- parse_file => parses the map_data.csv file
- get_territory_and_coord => takes the parsed data and returns the territories and coords for each territory
- create_dot => create an empty dot for each territory (used to ensure territory location entries are correct)
- run_create_units => runs the create_units class; returns the map used for the gui
"""
map_data = parse_file.parse_file(map_raw)
starting_units = parse_file.parse_file(starting_units_file)
num_of_units = len(starting_units)
units_on_map = run_create_units(starting_units, map_data, image_file)
creating_gui.display_image(units_on_map)


    


